<Page
    x:Class="CustomEffectsWithCapture.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CustomEffectsWithCapture"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical">
            <Button x:Name="captureVideo" Content="Capture Video" Click="captureVideo_Click"></Button>
            <Slider x:Name="SaturationSlider" Width="200" Minimum="0" Maximum="100" TickFrequency="5" Orientation="Horizontal" ValueChanged="SaturationSlider_ValueChanged" Value="0" />
            <CaptureElement x:Name="captureElement" Width="640" Height="480" />
        </StackPanel>
    </Grid>
</Page>
